import { SearchFieldPipe } from './search-field.pipe';

describe('SearchFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
